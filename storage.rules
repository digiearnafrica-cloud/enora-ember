rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Product images: anyone can view, only signed-in admin can upload/delete
    match /products/{imageFile} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
